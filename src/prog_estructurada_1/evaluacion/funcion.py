#Implementamos una funcion que haga el uso de return 
def suma(a,b):
    return a + b

#Llamamos a la funcion y guardamos el resultado en una variable
resultado = suma(5, 3)
print("El resultado de la suma es:", resultado)